CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

PROJECT(XML_TEST_PROJECT)

ADD_SUBDIRECTORY(extern)

INCLUDE_DIRECTORIES(extern/tinyxml2)

SET(CXX_FLAGS "-std=c++0x")

ADD_EXECUTABLE(test Main.cpp)
TARGET_LINK_LIBRARIES(test ankixml)
SET_TARGET_PROPERTIES(test PROPERTIES COMPILE_FLAGS ${CXX_FLAGS})

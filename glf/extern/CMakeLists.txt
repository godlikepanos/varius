# Lib
#
ADD_LIBRARY(glfextern dummy.c)

# GLEW
#
IF(GLF_GL_VERSION STREQUAL "GL3")
	MESSAGE("++ Adding GLEW")
	
	ADD_SUBDIRECTORY(GLEW)
	TARGET_LINK_LIBRARIES(glfextern glfglew)
ENDIF()

INSTALL(TARGETS glfextern DESTINATION ${GLF_LIB_INSTALL_DIR})
